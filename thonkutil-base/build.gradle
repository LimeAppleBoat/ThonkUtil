archivesBaseName = "thonkutil-base"
group = rootProject.group
version = getSubprojectVersion(project)

moduleDependencies(project, [

])
dependencies {
}
loom {
    accessWidenerPath = file("src/main/resources/thonkutil-base.accesswidener")
}