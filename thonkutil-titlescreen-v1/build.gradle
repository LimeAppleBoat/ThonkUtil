archivesBaseName = "thonkutil-titlescreen-v1"

version = getSubprojectVersion(project)

moduleDependencies(project, [
        "thonkutil-base"
])
dependencies {
}

loom {
    accessWidenerPath = file("src/main/resources/thonkutil-titlescreen-v1.accesswidener")
}