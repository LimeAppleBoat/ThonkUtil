archivesBaseName = "thonkutil-customization-v1"
group = rootProject.group
version = getSubprojectVersion(project)

moduleDependencies(project, [
        'thonkutil-base'
])
loom {
    //accessWidenerPath = file("src/main/resources/thonkutil-potions-v0.accesswidener")
}